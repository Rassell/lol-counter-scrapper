{
  "name": "lol_counter_scrapper",
  "version": "1.0.0",
  "description": "a lol counter scraper (sorry! we need some info)",
  "main": "Functions/lol-counter-scrapper/index.ts",
  "scripts": {
    "start": "npm run build && node dist/Functions/lol-counter-scrapper/index.js",
    "build": "tsc --project tsconfig.prod.json",
    "watch": "tsc && concurrently \"tsc -w\" \"nodemon dist/Functions/lol-counter-scrapper/index.js\""
  },
  "keywords": [],
  "author": "sammy the shark",
  "license": "ISC",
  "dependencies": {
    "@azure/functions": "^3.0.0",
    "axios": "^0.24.0",
    "cheerio": "^1.0.0-rc.10",
    "dotenv": "^10.0.0",
    "mongodb": "^4.2.2",
    "puppeteer": "^13.0.0"
  },
  "devDependencies": {
    "@types/node": "^17.0.0",
    "concurrently": "^6.5.0",
    "nodemon": "^2.0.15",
    "typescript": "^4.5.4"
  }
}
